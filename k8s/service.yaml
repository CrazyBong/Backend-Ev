apiVersion: v1
kind: Service
metadata:
  name: evchargefinder-api-service
  namespace: evchargefinder
  labels:
    app: evchargefinder-api
spec:
  type: ClusterIP
  selector:
    app: evchargefinder-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
